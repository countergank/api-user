version: '3.9'
services:
  api-user:
    image: countergank/api-user:${TAG}
    container_name: api-user-${TAG}
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
      target: development
    command: npm run start:debug
    ports:
      - ${PORT}:${PORT}
    env_file:
      - .env.${NODE_ENV}
    volumes:
      - .:/usr/src/app/${TAG}
    networks:
      - api-user

networks:
  api-user:
    name: api-user-${TAG}
    driver: bridge
