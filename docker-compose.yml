version: '3.9'
services:
  api-user:
    image: countergank/api-user:local
    container_name: api-user
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
      target: development
    command: npm run start:debug
    ports:
      - ${PORT}:${PORT}
    env_file:
      - .env.${NODE_ENV}
    volumes:
      - .:/usr/src/app
    networks:
      - api-user

networks:
  api-user:
    driver: bridge
